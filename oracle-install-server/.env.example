# 复制为 .env 后填写，切勿提交到版本库
# 可用 npm run keygen 生成随机值

# 后端监听端口
PORT=3000

# 监听地址。默认 127.0.0.1，只限本机访问。
# 确需内网访问时改为内网网卡 IP，并且必须配合防火墙，切勿使用 0.0.0.0 暴露公网。
HOST=127.0.0.1

# 接口访问凭据（Authorization: Bearer <token>）
API_TOKEN=

# SSH 私钥加密主密钥（32 字节 base64 或 64 位 hex）
MASTER_KEY=

# 日志级别
LOG_LEVEL=info
